# Every package.json script runs on Bun's runtime, not Node: `bun run dev` executes Vite under Bun.
[run]
bun = true

# src/ and dev/remote/ hold tests; the rest of dev/ is a browser-only gallery with none.
[test]
root = "."
